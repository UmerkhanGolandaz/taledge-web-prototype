// Exam track reuses the shared AI interview page. The component detects the
// /exam namespace from the URL and runs the exam-mentor interviewer persona,
// then advances to the exam Fit Score / readiness report.
export { default } from "@/app/student/[id]/interview/[mode]/page";
