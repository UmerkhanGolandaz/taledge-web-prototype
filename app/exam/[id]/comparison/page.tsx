// Exam track reuses the shared interview comparison report. It derives its track
// (exam vs placement) from the URL, so under /exam all navigation stays in the
// exam namespace.
export { default } from "@/app/student/[id]/comparison/page";
